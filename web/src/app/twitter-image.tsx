// The Twitter/X `summary_large_image` card reuses the root Open Graph image verbatim — one source
// of truth, one rendered asset. Next reads these named exports the same way for both metadata slots.
export { default, alt, size, contentType } from "./opengraph-image";
