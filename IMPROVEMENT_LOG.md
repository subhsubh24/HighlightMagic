# Improvement Log

Tracks every change merged to `main` by the autonomous factory loop.
Format: newest first. Only MERGED changes appear here.

| PR | Title | Track | Merged |
|---|---|---|---|
| #390 | a11y: raise the `--text-tertiary`/`Theme.textTertiary` token from white-opacity **0.4 → 0.5** so muted placeholder/helper text clears WCAG-AA. At 0.4 it rendered ~3.78:1 on `--bg-primary` (`#0F0A1A`) — below the AA-normal 4.5:1 floor — across ~59 web + ~36 iOS call sites (landing, editor, export, legal). 0.5 → **5.29:1** (passes) while staying visibly below `--text-secondary` (0.7 → 9.65:1), preserving the 3-tier hierarchy. Fixed at the shared token (web `globals.css` + iOS `Theme.swift`, which intentionally mirror each other) rather than per-usage; `brand-kit.md` token table updated. Adds `theme-contrast.test.ts` — reads the REAL `globals.css`, composites the translucent white over `--bg-primary`, computes WCAG contrast, asserts each tier ≥4.5 (+ tertiary alpha ≥0.5); proven to FAIL at 0.4 / pass at 0.5. **Supersedes** the Run 51/52 "leave the mirrored token untouched" rationale (see #375): the token-churn risk that parked it is now covered by the regression test + green build + independent design-taste review confirming the hierarchy survives. | design_taste / a11y | Run 54 |
| #378 | test(export): the first EXECUTING iOS export-to-file test (`Tests/HighlightMagicTests/ExportRoundtripTests.swift`). Every prior `ExportServiceTests` case asserts config constants only — nothing invoked the exporter, so a regression producing a broken file / wrong resolution / no file would pass CI silently. Synthesizes a real 640×480 H.264 source via `AVAssetWriter`, runs the production single-pass overlay export (caption+watermark, `filter:.none`), and asserts a playable output: file exists+non-zero, ends `.mp4`, decodable `AVURLAsset` with a video track + non-zero duration, transform-resolved render size EXACTLY **1080×1920**. Directly closes the most-cited ship-critical `functional_reality`/`tests_evals` gap (no runtime proof the core import→export→shareable-file journey works). Linux can't xcodebuild — gated by the required `ios` check. Merged this run (was auto-merge-armed at Run 53 housekeeping with `ios` still pending). | functional_reality / tests_evals | Run 54 |
| #380 | test(security): cover the previously-UNTESTED `web/src/middleware.ts` — the ROADMAP-D6 pre-launch SITE GATE (HTTP Basic Auth keyed on `SITE_GATE_PASSWORD`, with `/landing`,`/privacy`,`/terms`,`/support`,`/offline`,`/api/` exempt) and the Track-H6 per-request nonce CSP. 10 tests pin: gate OFF when unset; 401 on no/wrong/`Bearer`/malformed-base64 auth; ALLOW on correct password (any username); marketing+legal+`/api/*` stay public while a nested `/editor/…` path is still gated; CSP carries a fresh per-request nonce + `strict-dynamic` + no script `unsafe-inline`. Both reviewers mutation-tested (flip `===`, drop `/api/` exemption, invert gate-off default, inject `unsafe-inline`) — each broke the expected test. | Track H / tests_evals | Run 53 |
| #379 | fix(security): enforce the `/api/proxy-video` 100 MB size cap on ACTUAL bytes, not just the declared `Content-Length`. A chunked upstream omits Content-Length → `parseInt("")` is NaN → `NaN > MAX` is false → the guard silently passed the body into an unbounded `arrayBuffer()` (OOM on a public, rate-limited endpoint; the guard's own comment says it exists "to avoid OOM"). Replaced buffer-then-check with a streaming read that caps on the running byte total (memory bounded to ~the cap) + a fast-path declared-size reject. 2 regression tests: chunked passthrough works, and a chunked over-cap stream is aborted mid-read (reader cancelled) → 502 — proven to FAIL against pre-fix main. | Track H1/H2 | Run 53 |
| #375 | a11y: WCAG-AA contrast on landing microcopy + keyboard focus rings on footer links. `landing/page.tsx` "No credit card required"/"No spam, ever" microcopy and BOTH footers' link wrappers used `--text-tertiary` (~3.79:1 on `#0F0A1A`, fails WCAG AA 4.5:1); bumped to `--text-secondary` (~9:1). `Footer.tsx` links (and the landing inline footer) also gained the site's `focus-visible:ring` pattern (keyboard affordance). Per-usage fix only — the shared token (mirrors iOS `Theme.swift`) is untouched; decorative copyright stays tertiary. Closes the Run 51 deferred "landing microcopy a11y contrast" item. | design_taste / a11y | Run 52 |
| #376 | test(kinetic-text): cover the exit-animation TYPE branches (pop/slide/dissolve/fade) of `getKineticTransform`. The type (10th arg) is set per-clip from the AI plan's `captionExitAnimation` and drives the real export render (ExportStep `drawOverlays`→`getKineticTransform`, :2108); prior tests only hit the default `fade` branch. 5 tests pin each branch's distinct formula (pop scale↑, slide offsetY>0, dissolve α=t², fade offsetY<0, `popExitScale` override) — both reviewers mutation-tested them to confirm they catch a swapped formula. | tests_evals | Run 52 |
| #365 | test(security): cover the daily export-ceiling 429 path on `/api/score` + `/api/ios-score`. Both check `checkDailySpendCeiling` (per-user `DAILY_EXPORT_CAP=50`, the independent wallet backstop on the highest-frequency paid call) before the Anthropic fetch, but only the 402 monthly-quota path was tested — a regression dropping/reordering the 429 ceiling would ship green. Adds a behavioral test each: in-quota user at the cap → 429 + fetch never called, driven by the real `recordDailyExport` counter + `__resetCeilingStoreForTests`. | Track H7 / tests_evals | Run 51 |
| #363 | test(security): extend `generation-ceiling-block.test.ts` behavioral coverage from 4 to 11 paid routes. The wiring scan only proves a route *references* `enforceGenerationCeiling`; this suite proves it *honors* the 429 (short-circuits before the paid provider). Added animate/submit (Kling), outro/style-transfer/talking-head/upscale (AtlasCloud), music/submit + voice-clone (ElevenLabs) — every provider family. A reviewer reproduced the dropped-`return` regression to confirm the tests catch it. | Track H7 / tests_evals | Run 51 |
| #362 | fix(security): bound `frames`/`scores` on `/api/plan` + `/api/ios-plan` before the paid Sonnet planner call (they bounded `photoAnimations` but not these). `planFromScores` serializes `frame.base64` into a vision block + the scores into the text prompt, so an oversized array — or a few frames each carrying a huge base64 — inflates paid token cost unbounded within one ceiling-counted call. Adds `MAX_PLANNER_FRAMES` (=12,000) count cap + per-frame `anyFrameOverLimit(…, MAX_FRAME_B64_CHARS)` bound (key `base64` / `jpegBase64` per route), both 413 before the paid call. | Track H2 | Run 51 |
| #353 | test(evals): added ElevenLabs (`elevenlabs.eval.ts`) + AtlasCloud/Kling (`atlascloud.eval.ts`) real provider round-trip evals — closing the named ship-critical `tests_evals` G3 breadth gap (was Anthropic-only). The scoring rubric lives in a pure, unit-tested module (`eval-assertions.ts`: `checkTtsResult`/`checkVideoResult`/`decodeDataUriByteLength`) covered by 26 CI unit tests, so the pass/fail logic is verified even though the paid round-trips only run under `EVAL_MODE=1`. Also implemented the ROADMAP-mandated **per-run cost ceiling IN CODE** (`resolveEvalCostCapUSD`/`costCeilingExceeded`, `EVAL_MAX_USD` default $1) that aborts before any paid call — the hard prerequisite for the video eval; video eval double-gated (`EVAL_MODE=1` + `RUN_VIDEO_EVAL=1`). Corrected the `validation-manifest.ts` evalNotes + `docs/ci/VALIDATION.md` to point at the built files. Wiring into the weekly `.github/workflows/live-eval.yml` is owner-only (loop can't edit `.github/`) — see REMAINING_STEPS Phase 1, step 2c. | tests/G3 | Run 50 |
| #354 | perf(ios): `ThumbnailService` full-cleared its 50-entry cache (`cache.removeAll()`) at capacity, so every timeline scrub past 50 thumbnails wiped all 49 still-warm images and forced a cold-start `AVAssetImageGenerator` re-decode storm. Replaced with a real bounded LRU (parallel `accessOrder` array kept in lockstep, touch on hit/insert, evict only the coldest). Closes the named `performance` residual; all mutation stays actor-isolated (Swift 6 data-race-free). | performance | Run 50 |
| #350 | fix(credits): rewrote `VercelKVCreditStore.grant()` from a two-step SET-NX + INCRBY (an **in-doubt-write** window — `Promise.race` doesn't cancel the in-flight KV op, so a timed-out INCRBY could silently lose a paid pack, and a compensating rollback could DOUBLE-GRANT on an in-doubt success) to a **single atomic `kv.eval` Lua script** (claim marker + INCRBY commit together or neither). No split-write, no double-grant, no silent loss; `SET NX` stays the anti-mint boundary. Closes the `correctness_reliability` to-A+ residual. Live-KV round-trip queued in REMAINING_STEPS before the (dormant) lever goes purchasable. | correctness | Run 49 |
| #351 | fix(waitlist): wrapped every Vercel-KV op in `waitlist-store.ts` (sadd/set/get/del/scard) in the shared 5s `withTimeout` — it was the ONLY KV store missing it, so a KV hang would idle to the serverless hard-kill (serverless-budget hard rule). Also wrapped the confirm GET route's `confirmSignup()` in try/catch → branded 503 page (no enumeration leak) instead of Next's default error handler. Adds KV-path timeout tests. | correctness/H | Run 49 |
| #345 | docs(business-case): wrapped docs/BUSINESS_CASE.md's machine-readable summary under a top-level `BUSINESS_CASE_SUMMARY:` key so it parses identically to the other three dashboard feeds (QUALITY_SCORECARD:/GROWTH_STATUS:/GTM_SCORECARD:), and updated BOTH `scripts/preflight.sh` parse paths (python3+PyYAML and js-yaml node fallback) to read the nested key. Closes the `artifact_integrity` gap_to_a+. No numbers changed; verified both parsers return arr_year1.base=7740 from the awk-extracted block. | artifact_integrity | Run 48 |
| #346 | a11y(web): accessibility-wiring pass on the highest-traffic web surfaces — Turnstile CAPTCHA container gets role=group + aria-label (closes design_taste gap_to_a+, WCAG 1.1.1); Header reset-button + iOS-link get focus-visible rings (2.4.7); UploadStep error banner → role=alert/aria-live=assertive + dismiss button aria-label + 32px hit target + focus ring, "Clear all" focus ring (4.1.3/1.1.1); ResultsStep failed-animation banner → role=status (implicit polite — Reviewer A caught role=alert+aria-live=polite as contradictory, fixed). No visual change. Keyboard drag-reorder deferred. | A5 / design_taste | Run 48 |
| #347 | test(atlascloud): covered pollTaskResult (the outer polling loop every animate/upscale/style-transfer/photo-animation export awaits) — was entirely untested, only inner checkTaskResult was covered. 4 cases assert real outcomes: resolves URL on completion, rejects with the provider error on failure, rejects (never resolves undefined) on succeeded-with-empty-outputs, and loops through processing→complete (fake timers). 28/28 pass. | tests_evals / G2 | Run 48 |
| #342 | fix(honesty): removed the unbacked "Priority processing" Pro benefit from BOTH the landing pricing card AND `Sources/Utilities/AppStoreMetadata.swift` (the App Store Connect submission description — the real App Review surface). No tier/queue prioritization exists anywhere (verified web/src + Sources; only uniform IP rate-limiting), so the claim was vaporware and an App-Review false-claim risk. Both reviewers REQUEST_CHANGES on a web-only cut → extended to the Swift surface in the same coherent cross-surface honesty fix. | Honesty / store_readiness | Run 47 |
| #343 | feat(landing): added honest Schema.org SoftwareApplication JSON-LD structured data to `landing/layout.tsx` (real prices $0/$14.99/$149.99 mirrored from StoreKit config; NO fabricated aggregateRating/reviews since the app is unreleased). Enriches search + app-discovery snippets on the deliberately site-gate-EXEMPT /landing page. Reviewer A confirmed the strict nonce CSP does NOT block it (JSON-LD is a non-executable data block, exempt from script-src). | E (marketing/DOD2) | Run 47 |
| #340 | feat(growth): E8 experiment ENGINE — the last unbuilt Track-E infra. `web/src/lib/growth/experiments.ts`: deterministic sticky `assignVariant` (sha256(expId:unitId)→weighted bucket, no unit id stored), `recordEvent` KV-HINCRBY aggregate counters (in-memory fallback, only per-variant COUNTS never PII), `computeLift` two-proportion z-test with a MIN_SAMPLE_PER_ARM gate (insufficient_data below N, never noise-as-win). Public beacon `/api/growth/experiment` (rate-limited H1 + strict registry validation H2). Wired into the E7 analytics surface (getGrowthMetrics.experiments[]). Reviewer A caught + I fixed a real pooled≥1 NaN bug (conversions>exposures via a lost beacon); re-confirmed. 34 tests; coverage 90.49%. | E8 (growth engine) | Run 47 |
| #341 | test(spend-ceiling): covered 3 untested branches of the KV daily-ceiling wallet-drain backstop (Track H7) — TTL-set-only-on-first-write (a key with no TTL never resets the day's ceiling), best-effort-EXPIRE-failure (must not fail the caller closed → spurious 429), and withTimeout hung-INCR fail-closed (fake timers → 429). Test-only, no runtime change. | G2 (tests_evals) | Run 47 |
| #325 | test(frame-extractor): cover the visual scene-change interest-point branch. Follow-up to #323 — both prior reviewers flagged that the merged tests drove only the audio-onset half of the adaptive sampler, leaving the visual scene-change branch (frameDifference ≥ SCENE_CHANGE_THRESHOLD → `sceneChangeTimestamps.push`, midpoint anchoring at `time - effectiveInterval/2`) dark. Added a controllable canvas fake (`canvasOpts.sceneChange` → alternating 0x00/0xFF fills so every consecutive pair is maximally different) + a test proving scene changes ALONE (audio decode disabled) produce bonus frames at non-integer midpoint timestamps. Also closed the review nits: dropped a tautological assertion, asserted progress is non-decreasing across a mixed photo+video batch, and covered the photo image-load-error reject + `decodeVideoAudio`'s outer fetch-failure catch. frame-extractor.ts 95.65% → 97.82% stmts / 99.15% lines. Both reviewers APPROVED (Reviewer A empirically confirmed lines 345/368 flipped to covered). | G2 (tests_evals) | Run 46 |
| #324 | test(audio-mux): music/layer fetch-failure resilience + node-teardown coverage. Follow-up to #322 — lands the three tests the second reviewer requested + approved (auto-merge landed #322 on CI-green before they were pushed). `createAudioPipeline` has its OWN inline music fetch/decode (not `loadTrackAudio`): a music-fetch throw (render still builds, no music), a per-layer fetch throw (bad layer dropped, sibling still schedules — per-layer isolation), and a fake-timer assertion that `connectVideo`'s deferred `setTimeout(100)` actually disconnects the source + gain nodes (leak guard, guards against eager-disconnect cutting the fade). audio-mux.ts 95.34% → 99.22% stmts / 100% lines. Both reviewers APPROVED. | G2 (tests_evals) | Run 46 |
| #323 | test(frame-extractor): cover the browser frame/audio extraction orchestration. `frame-extractor.ts` (core import→detect pipeline entry) was 40.21% — a QUALITY_SCORECARD-named low-coverage ship-critical file "masked by the healthy aggregate." Pure DSP functions were tested; `extractFrames` / `extractFramesFromMultiple` / private `decodeVideoAudio` + `imageFileToBase64` were not. Added DOM + Web Audio test doubles (FakeVideo seeking, FakeCanvas capture, FakeAudioContext decode-with-onset-burst, FakeImage) driving the REAL orchestration: MAX_BASE_FRAMES_PER_VIDEO adaptive-interval cap (121 frames @600s vs 11 @10s), bonus frames + audio tagging around an onset peak, graceful no-audio degradation, progress 0→100, video load-error rejection, photo/video/mixed branches. 40.21% → 95.65% stmts (→97.82% after #325). No source change. | G2 (tests_evals) | Run 46 |
| #322 | test(audio-mux): cover loadTrackAudio + createAudioPipeline render graph. `audio-mux.ts` (ship-critical render path: mixes clip audio + music + voiceover/SFX into the export MediaStream) was 8.52% — a QUALITY_SCORECARD-named low-coverage ship-critical file. Only `mergeDuckSegments` + a constant were tested. Added a Web Audio API test double recording gain-automation/source-lifecycle/node-connection calls, asserting REAL scheduling logic: music load/loop/fade-in/fade-out envelopes, voiceover ducking (SFX correctly NOT ducking), audio breaths, per-clip `connectVideo` volume defaults + CORS-fallback muting, cleanup. 8.52% → 95.34% stmts (→99.22% after #324). No source change. | G2 (tests_evals) | Run 46 |
| #318 | fix(security): add a rotation-proof GLOBAL daily spend ceiling to the anonymous `/api/stems` route (Track H7). stems is the one paid ElevenLabs sub-op reachable from the PUBLIC web editor with NO userId (`/api/*` is site-gate-exempt), so a per-user ceiling can't apply; its only brake was the per-IP `PAID_RATE_LIMIT`, which `spend-ceiling.ts`'s own header notes is NOT rotation-proof. Its siblings all carry the H7 per-user daily ceiling; stems had nothing rotation-proof. Added `enforceGlobalGenerationCeiling(bucket, cap)` (a single shared counter under a DEDICATED `"global-gen"` CeilingKind) + `GLOBAL_STEMS_DAILY_CAP=200`; wired into stems after input validation, before the paid call. Reviewer A (cycle-1) caught a real keyspace-collision: the first cut used the `"gen"` kind, and userId is client-supplied unvalidated text on ~10 sibling "gen" routes → a forged `{userId:"__global:stems"}` could drain the bucket via an unrelated route; cycle-2 moved it to `"global-gen"` (structurally unreachable) + a collision-proof regression test. Both reviewers APPROVED. | H7 (security/wallet-drain) | Run 45 |
| #319 | feat(analytics): fire the `pricing_view` funnel event when the landing pricing section scrolls into view. `analytics.ts` defined+documented `pricing_view` but nothing emitted it (dead, defined event) — the visitor→pricing funnel step was uninstrumented. Wired via an `IntersectionObserver` on the pricing `<section>` (fires once at ≥30% visibility, then disconnects); no-op when analytics isn't loaded or `IntersectionObserver` is unavailable. Both reviewers APPROVED (real instrumentation-completeness gap, no visible UI change). | E5 (analytics/funnel) | Run 45 |
| #316 | docs(honesty): replace inaccurate "every frame" claims with accurate frame-sampling copy across the ASO listing, screenshot caption, social content scripts, and the marketing email. The detector samples ~1fps up to `MAX_BASE_FRAMES_PER_VIDEO=120` base frames (plus adaptive bonus frames) — it does NOT watch/analyze "every frame" (or "all N hours"). Corrected `aso-package.md`, `content/post-batch-1.md`, `content/post-batch-2.md`, `growth/email-sequences.md`; also tightened the ASO share line to name the iOS share sheet (matching the audited press-kit). Reviewer A's first pass caught an INCOMPLETE sweep (end-card "Frame-by-frame AI", "watched all 6 hours", "watched all of them", "watches every clip"); cycle-2 amend fixed all four; re-review confirmed complete. Both reviewers APPROVED. | E (marketing honesty) | Run 44 |
| #315 | test(waitlist): cover `addConfirmedSignup` — the DECISION-COROLLARY signup path used by DEFAULT when no email provider is wired (pre-launch state), previously 0 direct coverage. 4 outcome-asserting cases against the in-memory Set/Map fallback: direct confirm counts as both raw signup AND confirmed; idempotent for a repeated email (Set-dedup); confirms an email that first arrived pending without its token; orthogonal to double-opt-in (never consumes an unrelated pending token — that token still confirms). Both reviewers hand-traced assertions to the store's real semantics; 10/10 tests pass, lint clean. | G2 (tests_evals) | Run 44 |
| #314 | feat(seo): add the real public marketing routes `/landing` (0.8), `/support` (0.5), `/terms` (0.3) to `sitemap.ts` — previously only `/` and `/privacy` were listed, so the landing page + Apple-required support/terms pages were absent from the XML sitemap search engines consume. `/offline` (PWA fallback) intentionally excluded. Pre-launch SEO discoverability. Both reviewers APPROVED (routes verified to exist on disk). | E (growth/SEO) | Run 44 |
| #313 | fix(terms): clarify Pro is "unlimited **monthly** exports (a 50-per-day fair-use ceiling applies to all plans)" in the Terms of Service — was "unlimited exports", omitting the "monthly" qualifier and the `DAILY_EXPORT_CAP=50` ceiling that the ASO/landing FAQ/press-kit all disclose. Aligns legal copy with the actual entitlement (FTC / App Store consistency). Both reviewers APPROVED (ceiling verified in `spend-ceiling.ts`). | E (legal honesty) | Run 44 |
| #312 | fix(landing): accurate frame-sampling copy across all three landing-page surfaces (the "AI finds the magic" step, the AI Highlight Detection feature blurb, the "How does the AI work?" FAQ) — the live marketing page claimed the AI "analyzes every frame" / works "frame by frame", contradicting the ~1fps/120-frame sampler. Highest-traffic marketing surface; copy-only, no logic. Scouts had MISSED this (grep only caught "every frame" in docs); found via a follow-up repo-wide sweep. Both reviewers APPROVED. | E (marketing honesty) | Run 44 |
| #309 | test(validate): assert `/api/validate` honors its H1/P0/H7 wallet-drain guards. `/api/validate` is the pipeline's largest paid call (Haiku *vision* over every clip frame) yet was DELIBERATELY ABSENT from `paid-routes-rate-limit.test.ts` (H1) and `generation-ceiling-block.test.ts` (H7) — verified 0 matches for `validate` in both. Its wired per-IP rate limit, server-side quota gate (402/P0) and daily generation ceiling (429/H7) were never behaviorally asserted, so a regression dropping any (e.g. `checkExportAllowed`'s 402 no longer returned) would keep every grep/wiring reference green while silently re-opening an unbounded paid-vision path. 4 tests: per-IP flood→429; over-quota userId→402 (`upgrade:true`); ceiling→429 (proving quota→ceiling ordering); anonymous caller skips the userId-scoped guards and fails OPEN to `{passed:true}` on a rejected provider fetch (the control that keeps 1–3's `fetch`-not-called assertions from passing vacuously). Each spies `globalThis.fetch` (scoped+restored) to prove no paid call leaks. Test-only; 863 tests pass, lint clean, coverage floor held. Both reviewers APPROVED (Reviewer A traced each test to its unique branch). | G2/H (quality+security) | Run 43 |
| #299 | test(transitions): cover `drawTransitionOverlay` canvas render paths (transitions.ts ~16%→covered). The 188-line function renders the visual overlay for all 12 transition types on every previewed/exported clip (`ExportStep.tsx`, `TapePreviewPlayer.tsx`) but had zero coverage — a regression in an alpha curve, a phase boundary (`hard_flash` dark→blast→fade), a neon color, or an intensity scale would ship a visually-broken export silently. Behavioural-invariant tests via a minimal recording 2D-context stub (node env, no DOM): save/restore balance for all types, no-overlay types draw nothing, the flash midpoint-peak sine curve, `transitionIntensity` scaling, `hard_flash` three-phase progression, AI neon-color + `renderOptions` overrides, the `light_leak` 3-stop gradient, the `hard_cut` seam window. Test-only. Both reviewers APPROVED (Reviewer A's initial REQUEST_CHANGES was a false alarm from a hand-condensed diff I pasted; re-verified the actual committed file is tsc-clean → APPROVE). | G2 (quality) | Run 42 |
| #300 | test(post-processing): cover film-grain / vignette / film-stock canvas render paths (post-processing.ts ~33%→100% st/ln). `drawFilmGrain`/`drawVignette`/`applyFilmStock` run on every exported frame with a look applied but the canvas paths were untested. Tests via a recording main-ctx stub + a minimal `document`/canvas stub for the cached offscreen layers (cleaned up in `afterAll`): opacity/intensity no-op guards, grain drawn in `overlay` at the requested opacity, vignette intensity, the warm(sepia)/cool(hue-rotate)/contrast filter-string selection with reset-to-`none`, the `lighten` faded-blacks wash value `rgb(26,26,26)`, grain dispatch. Test-only. Both reviewers APPROVED. | G2 (quality) | Run 42 |
| #301 | test(kinetic-text): cover `drawKineticCaption` render + word-wrap paths (kinetic-text.ts ~31%→covered). The function renders every caption in the export/preview pipeline; only the animation math was tested. Tests via a recording 2D-context stub (`measureText` drives wrapping): empty/transparent no-op guards, save/restore balance, `verticalPosition`+`offsetY` placement, long-caption word-wrap preserving every word, distinct per-style fonts (Bold 900 / Minimal 300 / Classic italic Georgia), custom color+font override, per-character drawing when letter-spacing diverges from 1. Test-only. Both reviewers APPROVED. | G2 (quality) | Run 42 |
| #302 | fix(reliability): give the email/KV I/O routes an explicit serverless budget. `waitlist`, `waitlist/confirm`, `growth/stats` do external I/O (KV reads/writes + confirmation/welcome email sends; email client has its own 10s timeout) but declared no `maxDuration`, so they fell outside the paid-provider invariant (`route-maxduration.test.ts`) and inherited the ~10-15s platform default — a slow request could be killed mid-flow (signup persisted to KV, confirmation email never sent, dead-ending the pre-launch funnel). Added `export const maxDuration = 30` to all three + a dedicated fail-on-revert test. `maxDuration` is a ceiling (only consumed on a hang) so no steady-state cost; rate limits unchanged. Both reviewers APPROVED (both empirically verified the fail-on-revert). | B6 (reliability) | Run 42 |
| #283 | fix(H3): stop leaking provider error text on the paid audio/animation routes. `voiceover`, `sfx`, `music/submit`, `animate/check` returned the provider client's raw failure string (`"ElevenLabs TTS API error (429)"`, `"ElevenLabs API error (503)"`, AtlasCloud/Kling queue detail) straight to an unauthenticated client — naming the vendor + echoing the upstream HTTP status, enabling provider/rate-limit/health enumeration. Reviewer B found the same class on `/api/score` (`scoring failed (${resp.status})`, whose iOS twin `/api/ios-score` already generalizes it). Fix: log the raw error server-side (`console.error`, retained for ops), return a generic message with the same 502/failed shape; the animate poller keys off `status` not the error text, so the polling contract is unchanged. +tests on all 5 routes asserting no vendor/status leak (fail-on-revert). Both reviewers APPROVED (A reverted-in-a-scratch-worktree to confirm fail-on-revert; B re-reviewed the score addition → clean APPROVE). Verified already-clean: `credits/redeem` (generic `reason`), `talking-head`/`upscale`/`intro`/`outro` (generic; async failures surface via the now-sanitized `animate/check`); `proxy-video` status-echo left as-is (caller already knows the upstream URL — no marginal disclosure). | H3 (security) | Run 41 |
| #280 | fix(detect): bound the frame-scoring Anthropic call with a 45s timeout. `analyzeMultiBatch` (the web scoring server action reached from `DetectingStep`) called `api.anthropic.com` with NO `AbortSignal`, so a stalled upstream connection could hang detection until the serverless function was opaquely killed — same timeout-inversion class as the AtlasCloud/music fixes. Added `45_000` as `fetchWithRetry`'s 4th arg, matching the sibling `/api/score` route (`maxDuration=60`) that makes the identical Haiku vision-scoring call; a timeout is caught by `fetchWithRetry` (retries) then `analyzeMultiBatch`'s batch-level retry. Regression test asserts the fetch now receives an `AbortSignal` (undefined pre-fix). Both reviewers APPROVED after reverting `detect.ts` to confirm the test fails pre-fix. | B (reliability) | Run 40 |
| #279 | test(elevenlabs-voice-clone): cover the voice-cloning paid client (was 0% covered). `createVoiceClone`/`deleteVoiceClone`/`generateWithClonedVoice`: multipart `voices/add`+voice_id, OK-but-missing-voice_id → failed, non-OK → failed (no throw), missing-key throw, `DELETE` cleanup swallows network errors (never masks the export result), cloned-voice TTS endpoint/model_id (`eleven_flash_v2_5`)/body+duration, non-OK/empty → failed. Test-only; completes the paid ElevenLabs client coverage set. Both reviewers APPROVED. | G2 | Run 40 |
| #278 | test(elevenlabs-stems): cover the stem-separation paid client (was 0% covered). `separateStems`/`isolateInstrumental`: multipart audio-isolation POST with the instrumental aliased across drums/bass/other stem slots (vocals `""`, per the module's documented limitation), Blob+Buffer inputs, non-OK → failed (no throw), empty 0-byte → failed, missing-key throw, malformed-data-URI short-circuit (no network call), failure propagation. Test-only. Both reviewers APPROVED. | G2 | Run 40 |
| #277 | test(elevenlabs-music): cover the AI-music paid client (was 0% covered). `generateMusic`: compose endpoint/headers, COGS metering by requested track length, clamp `[3s,300s]`, NaN→60s default, the `bad_prompt` single-retry-with-suggestion recursion (retries exactly once with ElevenLabs' sanitized suggestion, then fails — proves the `_isRetry` guard bounds recursion), generic API-error → failed (no cost metered), empty buffer → failed, missing-key throw. Test-only. Both reviewers APPROVED (B: highest-value of the four — covers the infinite-recursion risk). | G2 | Run 40 |
| #276 | test(elevenlabs-sfx): cover the sound-effects paid client (was 0% covered). `generateSoundEffect`/`generateSoundEffectBatch`: sound-generation endpoint/headers, COGS metering by requested duration, clamp `[500ms,10000ms]`, NaN→2000ms default, non-OK → failed (no throw, no cost metered), empty 0-byte → failed (no cost metered), missing `ELEVENLABS_API_KEY` throws, parallel batch preserves order+per-request duration. Test-only; extends the paid-module coverage begun with tts/scribe (#270/#271). Both reviewers APPROVED. | G2 | Run 40 |
| #274 | feat(paywall): surface the annual tier at the free-limit paywall moment. The web limit-hit paywall CTA read only "Upgrade to Pro — $14.99/mo", omitting the LIVE annual plan the landing page already advertises — a missed higher-LTV/higher-margin lever (BUSINESS_CASE §9) at the highest-intent conversion moment, and a cross-surface inconsistency. Button → "Upgrade to Pro" (action) + a price caption "$14.99/mo · $149.99/yr (2 months free)" (`-mt-5` tucks it under the CTA; `text-secondary`/`text-tertiary` hierarchy). Prices verified vs landing/page.tsx + SubscriptionProduct.swift + StoreKitConfiguration.storekit (all agree); "2 months free" = $14.99×12=$179.88 vs $149.99. No behavior/href change. Both reviewers APPROVED (A verified prices across 3 sources; B did the designer-question on the caption). | Monetization/F8 | Run 39 |
| #273 | fix(scoring): guard the upstream response.json() on the frame-scoring proxies. `/api/score` and `/api/ios-score` checked fetch + `!resp.ok` but parsed the Anthropic response with an UNGUARDED `await resp.json()` — a 200 with a truncated/corrupt body (proxy/CDN glitch) threw uncaught: score fell out as a 500 instead of its graceful 502; ios-score threw straight out of its retry loop instead of retrying (like it does for a thrown fetch/429/5xx). score → wrap → 502; ios-score → `lastError=…; continue` (retry), throw cleanly only after retries. +2 regression tests (corrupt 200→502 for score; corrupt-then-good→200-with-retry for ios-score). Both reviewers APPROVED after empirically reverting the fix to confirm the tests fail without it; confirmed no quota burned on the failure path (consumeExport runs later). | B/reliability | Run 39 |
| #272 | fix(atlascloud): retry an unparseable 200 body instead of aborting the export. submitTask + checkTaskResult already retry a thrown fetch and HTTP 5xx (#238/#245), but `response.json()` on the SUCCESS path was unguarded — a corrupt/truncated 200 (CDN/proxy glitch) threw straight out of the retry loop, aborting the whole submit/poll (and the paid export) on a transient blip the retry logic was built to absorb. Both parses wrapped: treat a parse failure like a 5xx/thrown fetch (retry with the same backoff, give up only after MAX_RETRIES). +2 fake-timer tests (submit + poll: a rejecting json() retries, the second attempt returns the parsed result). Reviewer B noted the pre-existing duplicate-submit-on-retry exposure is unchanged by this diff (no idempotency key, same as the accepted #238 pattern) — flagged as a future follow-up, not introduced here. | B6/reliability | Run 39 |
| #271 | test(elevenlabs-scribe): cover the transcription client (was 0% covered). elevenlabs-scribe.ts transcribes audio + feeds word-level timing into Claude's planning prompt — silent parse/segmentation failures degrade highlight quality with nothing to catch a regression. Behavior-asserting tests (mocked fetch): words group into segments splitting on a pause `>1s` (sub-1s gaps stay together, asserted with concrete boundary values); missing word confidence → 1.0; no words → empty segments with `data.text` preserved; `data.text` absent → text reconstructed from segments; API error → `{status:"failed"}` (not a throw); missing key throws. Test-only; both reviewers cross-checked every assertion against the source loop. | G2/tests | Run 39 |
| #270 | test(elevenlabs-tts): cover the voiceover TTS client (was 0% covered). Paid-API integration on the export path with no tests — failed generations waste credits, and untested error paths could return a broken data URI or silently skip COGS metering. Tests (mocked fetch + usage-meter): resolveVoiceId maps known chars + falls back to default (no crash); generateVoiceover returns completed audio + estimated duration, hits the resolved voice endpoint with the api-key header, and METERS COGS per input char; an API error returns `{status:"failed"}` (not a throw) and meters NO cost; a 0-byte response is rejected (no broken data URI); a missing key throws; generateVoiceovers preserves the clipIndex mapping. Test-only; Reviewer A mutation-tested (5 injected regressions each caught by exactly one test). | G2/tests | Run 39 |
| #269 | test(credit-store): cover the security-critical VercelKV credit path. The in-memory store was tested but VercelKVCreditStore — the production path carrying the redemption-idempotency guard + balance arithmetic a client could abuse (replay one purchase to mint credits; over-spend past zero) — had ZERO coverage. Mock @vercel/kv + stub env to force the KV store and assert: grant() sets the SET-NX marker with the ~13-month TTL then INCRBYs; a replayed transactionId (NX refused → null) grants NOTHING (the anti-mint guard); getBalance floors null/negative at 0; consumeOne returns the atomic DECR and clamps a negative over-spend to 0 even when the clamp SET throws; a hung KV op rejects via the timeout (fail-closed). Test-only; both reviewers confirmed each test is falsifiable against the real branch. | H/security tests | Run 39 |
| #248 | fix(content): rewrite content-batch drafts to match shipped v1 (honesty/FTC). Closes the 4-run-carried follow-up. `docs/content/post-batch-{1,2}.md` were pervasively built on features v1 does NOT have — AI-generated MUSIC/SFX/voiceover, "custom music scored to your clip", "no copyright strikes" (music-gen), and a bare "Unlimited exports" contradicting the monthly-cap + 50/day fair-use reality. Verified against shipped code (music picker exists in UI but is NON-FUNCTIONAL: `git log --all --diff-filter=A` shows ZERO audio files ever committed → `MusicTrack.bundleURL`=nil always → mix branch unreachable; ElevenLabs `isAvailable=false`). Batch-1 POST 04 (fictional "Music+SFX Auto-Sync") rewritten to a real frame-scoring demo; POST 06 reason 2 ("AI generates music")→"no captions/sound-off viewing"; concert "AI music=no copyright strikes" removed; "15+ caption styles"→7 (KineticCaptionStyle); all "with music" b-roll→captions/transitions. Batch-2 POSTs 01/03/04/06/07/10/12 music/SFX/sound-design→cuts/captions; bare "Unlimited"→"no monthly export cap"; the Usage Note that ESTABLISHED music as a feature replaced with an explicit "NO working music/SFX/voiceover in v1" note. A reviewer wrongly claimed the music library is live (code-structure read, no asset check) + asked to reintroduce the claim — verified objectively via git and overrode; fresh re-review APPROVED. No business-case impact (music never a lever). | Store/honesty | Run 38 |
| #247 | test(web): add dedicated tests for the /api/plan quota gate + SSE paths (G2). The web `/api/plan` route (priciest paid call — Sonnet planner, 1–3 min) had NO dedicated tests — only the generic rate-limit sweep — while its iOS twin `/api/ios-plan` is fully covered. A regression dropping the P0 quota gate before the paid call would be a silent wallet-drain nothing caught. New `plan-route.test.ts` (SSE-adapted): 400 missing-userId/non-array; 413 oversized creativeDirection (H2); 402 over-quota asserting BOTH `planFromScores` AND `enforceGenerationCeiling` are never reached (gate before the stream); 200 SSE streams `event: result`; planner-throw streams a GENERIC `event: error` with no raw upstream leak (H3); H7 daily ceiling blocks; 429 rate limit (H1). A reviewer mutation-verified the 402 test FAILS if the gate is removed. Tests only. | G2/tests | Run 38 |
| #246 | feat(cost): meter /api/ios-score Anthropic spend + add retry-behavior test (B2/COGS). `/api/ios-score` is the PRIMARY paid Anthropic call on the iOS launch path but was the one frame-scorer route with NO `[CostMeter]` log (web `/api/score` has it) — the main cost driver was invisible in Vercel logs. `scoreBatchWithHaiku` now returns `{scored,usageIn,usageOut}`; the POST handler sums usage across frame batches and logs one per-export `[CostMeter]` line matching the web format (model + in/out tokens + `estimateCostUSD`). Observability only — no model/behavior/quality change. +2 tests: metering line fires with the right tokens + real $ estimate; a transient 429 is retried then succeeds (the load-bearing retry loop was previously only happy-path tested). | B2/COGS | Run 38 |
| #245 | fix(atlascloud): retry a THROWN submit fetch instead of aborting the export (B6). `submitTask()`'s fetch was not wrapped in try/catch: a thrown fetch (network/DNS/socket blip or the per-attempt `AbortSignal.timeout` firing) propagated straight out of the retry loop, killing the whole submit (and the export) on the first hiccup — the loop only retried HTTP 502/503/504. This is the exact class #238 fixed for the poll path (`checkTaskResult`); mirrored on the submit path: catch a thrown fetch, retry with the same exponential backoff subject to the 55s overall budget, give up only after retries exhausted; non-retryable HTTP still fails fast (untouched). +2 fake-timer tests (retry-then-succeed=2 calls; give-up=4=initial+MAX_RETRIES); a reviewer traced the budget arithmetic to confirm the call counts. | B6/reliability | Run 38 |
| #244 | fix(landing): correct false "or photos" import claim + add footer link focus rings. Two corrections on the LIVE landing page. (1) Honesty (store/FTC): "How it works" step 1 said "Pick videos or photos from your camera roll" but the iOS import picker is `matching: .videos` (Sources/Views/Screens/HomeView.swift:52) — v1 has NO photo import; corrected to "Pick videos". (2) A11y: the 4 footer links (Privacy/Terms/Support/iOS App) had zero focus-visible styling while the header nav links already had it — added the identical `focus-visible:ring-2 ring-[var(--accent)]/60` treatment. Copy + className only; no structural change. Reviewer verified `replace_all` hit exactly the 4 footer links (line-375 CTA untouched) + HomeView.swift:52 confirms videos-only. | Store/honesty + a11y | Run 38 |
| #243 | fix(security): rate-limit the public /api/proxy-video endpoint (Track H1). `/api/proxy-video` is a PUBLIC, unauthenticated GET that buffers up to 100 MB of upstream bytes per request in the serverless function; it had SSRF defenses (HTTPS-only + domain allowlist + size cap) but NO rate limiting — the one paid/expensive route the H1 sweep had missed (an unthrottled flood = unbounded bandwidth/egress + memory drain). Added a per-IP throttle at the TOP of `GET` (before any parsing/fetch) using `POLL_RATE_LIMIT` (60/min/IP — the module's documented GET-amplification tier; generous for a legit multi-clip export, tight for a flood; not the 10/min PAID tier which would break export rendering). +2 tests (429-after-limit via the url-missing path; per-IP isolation); `_resetBuckets` in beforeEach. Both reviewers noted the residual per-instance/XFF caveats are pre-existing systemic properties of rate-limit.ts. | H1/security | Run 38 |
| #239 | fix(docs+legal): honest public capability across ALL surfaces — v1 does not ship AI audio/video generation. Five public-facing surfaces (README, press-kit, privacy policy, terms of use, support FAQ) overclaimed shipped capability: the ElevenLabs (music/SFX/voiceover/voice-clone) + AtlasCloud (photo→video, intro/outro, style-transfer) integrations exist in the backend but are DORMANT in v1 (iOS services hard-return `isAvailable=false`; import picker is `matching: .videos` so there is NO photo import). v1's only live third-party AI flow is Anthropic/Claude highlight-detection frame scoring. README dropped `music/SFX/voiceover` from the shipped workflow + labelled the arch bullets "not enabled in v1"; press-kit narrowed the AI claim; the PRIVACY POLICY (the URL README/press-kit cite — direct legal/FTC weight) reframed the ElevenLabs/AtlasCloud flows across the "what leaves your device" list, the "third-party AI providers" section, AND the data-retention note as conditional/future ("not enabled in the current version"); terms §2/§6 scoped to the live Anthropic flow; support FAQ removed the false "Photos … supported for animated photo highlights" line. Two review rounds + a final confirmation review (all verified vs source: both services isAvailable=false, picker .videos) — a full `web/src/app` sweep confirms no remaining un-hedged overclaim. Closes a store-review/FTC honesty risk on every public surface. | Store/honesty | Run 37 |
| #238 | fix(atlascloud): retry a thrown poll fetch (network/DNS/timeout) instead of aborting the export. `checkTaskResult()` retried only on HTTP 502/503/504; a THROWN fetch (socket/DNS blip or the `AbortSignal.timeout` firing on a poll) propagated straight out with no retry, killing the whole polling loop (and the export) on the first transient hiccup — the QUALITY_SCORECARD correctness→A+ gap. Wrapped ONLY the `fetch()` so a thrown network error is treated like a transient 5xx (same bounded backoff, up to MAX_RETRIES) and only gives up once retries are exhausted; the HTTP-status fail-fast logic is untouched (a non-retryable status still fails fast, not caught-and-retried). +2 tests (throw-then-succeed retries; keeps-throwing gives up after 4 attempts) with fake timers. Both reviewers verified semantics preserved + accounting correct. | Reliability/G | Run 37 |
| #237 | feat(revenue): export credit-pack lever — durable credit balance + idempotent StoreKit redemption + gate fallback (BACKEND HALF). Business-case lever (b): a free user who exhausts the monthly free-export limit can buy a consumable credit pack instead of subscribing to Pro; each credit buys one extra export, credits never expire — directly couples revenue to per-export COGS + captures non-subscribers. `credit-store.ts` (new): durable per-user KV balance (atomic INCRBY/DECR + SET-NX idempotency marker), in-memory fallback, 5s timeout, fail-closed. `entitlement.ts`: `redeemCreditPack()` cryptographically verifies the StoreKit consumable JWS (same Apple-anchored path as Pro; no expiry for consumables), maps productId→credit COUNT, rejects refunds, idempotently grants keyed on Apple's transactionId (a replay can NEVER mint credits twice); `checkExportAllowed()` falls back to credits once the monthly quota is exhausted (extra KV read only on the over-limit path; fails closed); `consumeExport()` spends a credit instead of the maxed monthly counter over the limit. `constants.ts` CREDIT_PACK_PRODUCTS (10/30/100; prices in App Store Connect). New `/api/credits/redeem` route (rate-limited H1, input-bounded H2, generic errors H3). 26 new tests. iOS StoreKit consumables + purchase UI = owner half (REMAINING_STEPS). Both reviewers APPROVED (correctness+security, value+cost). | Track F (F8 lever) | Run 37 |
| #233 | fix(store): AppStoreMetadata honesty — remove dormant music/SFX/voiceover claims (0e). `Sources/Utilities/AppStoreMetadata.swift` (the metadata the owner copies into App Store Connect) had App Review Notes falsely telling Apple's reviewer that music/SFX/voiceover are generated by ElevenLabs and intro/outro cards by AtlasCloud (Kling) — all hard-disabled/dormant in v1 (#180; REMAINING_STEPS 0d), and no music files ship (MusicTrack.bundleURL always nil). A reviewer who tests these = rejection risk; the consumer-facing "14 royalty-free music tracks / cinematic LUTs / particle overlays / Unlimited exports" claims = FTC/accuracy risk. String-literal-only rewrite (iOS-conservative; merged green through the required `ios` check) aligned to the already-vetted honest ASO/press copy: Review Notes now state editor features (trim, kinetic captions, color filters, templates) are deterministic + on-device and the only server-side AI is frame-scoring detection; removed all music/LUT/particle claims from description/whatsNew/plans/screenshots (music-picker screenshot → style-templates); "Unlimited exports" → "Unlimited monthly exports (50/day fair-use ceiling)". Independent honesty reviewer verified every retained claim vs source (8 templates=TemplateLibrary; kinetic/filter enums real; premium effects=real PremiumEffectRenderer; 7-pass literal; iCloud wired) — no new inaccuracy. Both reviewers APPROVE. Closes 0e (store_readiness honesty) | Store/honesty | Run 36 |
| #232 | fix(web): move the per-user daily spend ceiling to KV — close cross-instance wallet drain (H7). The Track-H7 daily EXPORT/GENERATION ceilings were in-memory PER Vercel serverless instance; the paid generation sub-calls (intro/animate[Kling]/sfx/voiceover/…) are NOT covered by the KV-atomic monthly export quota, so the daily ceiling was their only per-user wallet-drain backstop — and per-instance it multiplied by the instance fan-out (rotate IPs + hit different instances → cap × N). Moved to a Vercel-KV store (atomic INCR + 2-day EXPIRE on a UTC calendar-day key), InMemory fallback when KV env absent (dev/tests), 5s timeout < serverless budget, FAIL CLOSED on KV error (consistent with the entitlement gate). enforceGenerationCeiling now counts at admission via atomic INCR (removes the check-then-act boundary race); first-write EXPIRE best-effort (a TTL hiccup can't spuriously 429 a legit user). Public API became async → added `await` at all 17 paid-route call sites (verified none left sync). Rewrote spend-ceiling.test.ts for async + calendar-day semantics + added fail-closed-on-KV coverage. Tradeoff (documented in-module): UTC-day bucket resets at 00:00 UTC (a midnight-straddling burst can reach ~2× cap — bounded, rare) — the price of an atomic cross-instance counter; mirrors the calendar-month monthly quota. Build ✅ · 721 tests ✅ · coverage above floor ✅ · 0 lint ✅ · both reviewers APPROVE. Follow-up (pre-existing, non-blocking): the export-ceiling path still does a separate check-then-record (narrower race, gated first by checkExportAllowed) | H7/security | Run 36 |
| #229 | test(web): enforce the coverage floor in CI + cover the app-state reducer. The vitest.config.ts coverage thresholds (stmts/funcs 60, branches 50, lines 60) gated NOTHING — @vitest/coverage-v8 was never installed and CI's `npm test` ran `vitest run` with no --coverage (the ship-critical tests_evals gap the QUALITY_SCORECARD names). Installed @vitest/coverage-v8@4.1.9 (matches resolved vitest@4.1.9) and flipped the `test` script to `vitest run --coverage`, so the required `web` job now FAILS a merge on any coverage regression — no .github/ edit (a separate test:coverage script would never run in CI; flipping the default is the only mechanism). Added 24 genuine reducer tests (store.ts 37.5%→~85%): SET_VIDEO url-dedup, ADD_MEDIA legacy-video derivation incl. animated-photo fallback, REMOVE/REORDER/CLEAR_MEDIA guards (out-of-bounds returns the SAME state ref, asserted via toBe), clip update/reorder, animation-flag toggles, per-index UPDATE_SFX_TRACK/UPDATE_VOICEOVER_SEGMENT, music-null-on-failure, unknown-action passthrough. Measured after: stmts 62.91/branch 58.2/func 75.77/lines 63.36 — clears floor with margin; 718 tests, 0 lint. Both reviewers APPROVE (correctness cross-checked line-by-line vs reducer; value verified non-padding). Advances G2 (web half only; iOS XCTest floor separate, not claimed) | G2/tests | Run 35 |
| #225 | fix(landing): correct the landing FAQ's "synced audio" claim to real v1 features. The "How does the AI work?" answer said reels are assembled "with transitions, captions, and synced audio" — in a feature list "synced audio" reads as an added audio capability, but music/beat-sync is non-functional (no bundled audio assets → MusicTrack.bundleURL always nil → BeatSyncService synthetic grid) and AI audio is hard-disabled (#180); the only export audio is the source clip's own track. Replaced with smooth transitions, animated captions, and color filters (all real+reachable, both reviewers verified). Closes the false-claim class on the highest-traffic marketing surface | D/honesty | Run 34 |
| #224 | fix(marketing): remove non-functional music claims + correct inflated counts across ASO/press/content (follow-up to #223, which auto-merged on CI before the review round finished). Music is non-functional in the repo (ZERO .mp3/.m4a/.wav ever committed → bundleURL always nil → insertion unreachable → synthetic beat grid), so removed all "beat-matched music/curated royalty-free library/auto-matched" claims (like SFX/voiceover) and replaced with the real smooth-transitions feature; corrected "15+ caption styles"→7 (KineticCaptionStyle) and "20+ color filters"→10 (VideoFilter). Both reviewers verified numbers + transitions against code | D/honesty | Run 34 |
| #223 | fix(marketing): align ASO/press/content with shipped v1 capability — remove dormant AI SFX/voiceover/voice-clone claims (ElevenLabs-only, hard-disabled #180) from the ASO description (pasted verbatim into App Store Connect = reviewer-rejection + FTC risk), press kit, and content calendar; "Every feature included"→"All core features included". Same honesty class as landing #215. NOTE: auto-merged an intermediate version before the review round completed (still over-claimed music + inflated counts) → corrected in #224 | D/honesty | Run 34 |
| #222 | test(web): behavioral proof that the Track H7 daily generation ceiling actually BLOCKS. The fleet-wide generation-ceiling-wiring test is a SOURCE SCAN (proves the call is present, not honored) — a regression that calls enforceGenerationCeiling() but drops the `return genBlock` short-circuit stays green while re-opening the unbounded wallet-drain path. New suite mocks the ceiling to 429 and asserts sfx/voiceover/intro/thumbnail (both provider families) return 429 AND never reach the paid provider, + a control. Mutation-verified by maker AND both reviewers | H/tests | Run 34 |
| #217 | test(web): cover the /api/sfx pre-generated-library cache-hit fast-path — a library hit returns instantly/free and short-circuits BEFORE the quota gate + paid ElevenLabs generator, but the path was unexercised (the route test's lookupSfxLibrary mock defaulted to a miss), so a regression in ordering (gate moved ahead of the free lookup → free asset burns quota+COGS) or response shape would ship silently. Force a hit + assert 200/completed with the library url/duration AND that generateSoundEffect + checkExportAllowed are never called. Both reviewers mutation-verified it's not a false-green. No prod change | G/tests | Run 33 |
| #216 | test(web): fleet-wide invariant — every API route importing a paid provider must call enforceGenerationCeiling() or checkDailySpendCeiling() before the paid call (Track H7 wallet-drain wiring). The ceiling fns were unit-tested in isolation but the WIRING was unasserted, so removing a guard from a route (or adding a new paid route without one) was a silent unbounded-spend hole. Mirrors route-maxduration.test.ts: discovers 19 provider routes, requires a ceiling on 17, with a justified CEILING_EXEMPT allowlist (animate/check poll-only no-userId; stems no-userId per-IP bound) + a >=15 discovery tripwire. Reviewer A mutation-verified. No prod change | H/tests | Run 33 |
| #215 | fix(landing): stop advertising dormant AI-audio features on the marketing page (store/FTC accuracy) — AI Music, SFX, Voiceover, Voice Clone were marketed as live (2 feature cards + a hero line + included paid-tier benefits) but are DORMANT in iOS v1 (#180 hard-disabled the direct-provider path: apiKey→nil/isAvailable→false; not yet backend-routed, REMAINING_STEPS 0d). Removed the 2 cards + 2 pricing lines + softened the hero word + dropped unused Music/Mic icons; reflowed the 4 remaining cards to lg:grid-cols-4 (Reviewer B caught a 3+1 grid orphan). No business-case impact | D/honesty | Run 33 |
| #214 | fix(web): harden the freemium quota gate against Vercel KV outages (B6/H) — VercelKVQuotaStore.get/incr made a network round-trip with NO timeout + NO try/catch; in prod a transient KV failure threw out of checkExportAllowed/consumeExport, uncaught in 5 routes with no outer try (score/ios-score/plan/ios-plan/ios-validate) → a raw 500, and consumeExport (post-paid-run) turned a delivered export into a 500 + double-spend on retry. Add KV_OP_TIMEOUT_MS=5s (under the 30s shortest budget); checkExportAllowed fails CLOSED on a read error (Pro returns before the read, unaffected); consumeExport swallows a write error. +11 tests (fail-closed, no-throw consume, KV timeout via fake timers) | B/reliability | Run 33 |
| #212 | fix(web): drop the false "offline AI / 100% on-device processing" pitch on the PWA offline fallback page (offline/page.tsx) — all paid AI runs server-side (README) and the iOS app also needs a connection for AI, so the claim was App Store/FTC-inaccurate. Replace with an honest CTA ("Prefer a native experience? Get the iOS app.") keeping the App Store link; copy-only. Sibling of #209 (flagged by its review) | D/honesty | Run 32 |
| #211 | fix(web): cap the ElevenLabs music-generation abort budget UNDER the /api/music/submit route's 60s maxDuration (B6 timeout-inversion) — generateMusic used AbortSignal.timeout(120_000) on a synchronous route, so the abort was dead code and a stalled call surfaced as an opaque "function timed out" instead of a clean 502. Extract MUSIC_GENERATION_TIMEOUT_MS=55_000 + a regression test that spies on AbortSignal.timeout to prove the const reaches the live fetch. Same class as #203. No COGS change | B/reliability | Run 32 |
| #210 | fix(privacy): declare the four genuinely-collected data types in PrivacyInfo.xcprivacy (was an empty NSPrivacyCollectedDataTypes array — an App Store privacy-manifest rejection risk while the app uploads data) — PhotosorVideos (video frames), UserID (anonymous Keychain id), OtherUserContent (prompt text), PurchaseHistory (StoreKit JWS on Pro requests); each Linked=true/Tracking=false/AppFunctionality. Identifier strings verified against Apple's machine-readable spec (lowercase "or" in PhotosorVideos). Closes the scorecard store_readiness privacy-manifest gap | D/store-readiness | Run 32 |
| #209 | fix(web): correct the misleading "offline AI" Pro benefit in the ExportStep "Go Pro on iOS" upsell card — all paid AI is server-side, so Pro adds no offline-AI capability (App Store/FTC accuracy). Replace with an accurate benefit ("premium effects") matching the iOS PaywallView "Exclusive filters & effects". Copy-only | D/honesty | Run 32 |
| #205 | docs(business-case): close the independent scorecard's business_case_strength A→A+ gap — add a §4 modeling note explaining the Pro-subscriber column is a snapshot 3%-of-MAU share (tracks MAU's 10%/mo), NOT an accumulating cohort. Reviewers caught + I fixed an honesty defect: rather than a false "above or below" symmetry, the note now states the verified direction — a new-user cohort waterfall (3% of new users, 4.5% churn) yields ~24% fewer subs at M12 / ~31% by M36 (in-loop recomputed: ≈32 vs 43, ≈292 vs 423), so the base case is the OPTIMISTIC end of that range. Invented "Section 1/F7" ref removed. Clarity-only; no table number changed, as_of unchanged | F/honesty | Run 31 |
| #204 | test(web): broaden the fleet-wide B6 maxDuration guard's PROVIDER_MARKERS to also match the @/lib/kling wrapper (animate/*) and @/actions/detect action (plan/* → Anthropic planner) — previously only DIRECT provider imports were scanned, so a future route under those wrappers could ship with no serverless budget and be silently killed in prod. The 4 existing such routes already declare a budget → guard stays green (16→20 checked routes); re-lands the Run-30 broadening lost to a fast-merge | B/reliability | Run 31 |
| #203 | fix(web): bound the AtlasCloud submitTask timeout UNDER the 60s submit-route serverless budget (B6 timeout-inversion) — the 120s per-attempt AbortSignal + unbounded retry loop could never fire before Vercel's ~60s kill, so the timeout/retry was dead code and users got an opaque "function timed out". Cap each attempt at 50s + the whole call at a 55s budget via a pure, tested submitAttemptTimeoutMs() helper; skip a retry that would blow the budget. No COGS change | B/reliability | Run 31 |
| #197 | docs(business-case): fix the last two stale "50-export/MONTH cap" refs (§5 $99.99/yr viability sentence + §9 annual-tier checklist item 4) that Run 25's Lever-4 correction missed — both now describe the ACTUAL shipped control (50/user/DAY anti-abuse ceiling, spend-ceiling.ts) and state Pro is unlimited-monthly, so the thin $99.99/yr heavy-usage margin is unbounded (why $149.99/yr is recommended). Consistency/honesty only; no model recompute, as_of unchanged | F/honesty | Run 30 |
| #196 | fix(web): give /api/ios-validate the same 45s Haiku-vision budget as the web /api/validate route (was 20s timeout / 30s maxDuration vs 45s / 60s) — the identical vision call under load takes 15-25s, so the iOS path aborted legit validations into fail-open (validation silently skipped) far more often than web, an asymmetric iOS export-quality regression. Vision timeout 20→45s, text 15→30s, maxDuration 30→60 (B6) | B/reliability | Run 30 |
| #195 | fix(web): declare maxDuration=60 on the 4 paid provider routes that had NO serverless budget (style-transfer/thumbnail/upscale → Atlas Cloud submit; voice-clone → ElevenLabs) — on Vercel they inherited the ~10-15s platform default and were killed mid-upload (silent BUILDS≠WORKS/B6: "request failed", lost export). Parity with the 14 sibling paid routes + a fleet-wide regression guard (route-maxduration.test.ts) so a future paid route can't ship without a budget | B/reliability | Run 30 |
| #189 | security(H): rate-limit the unauthenticated /api/animate/check status poll (NEW generous POLL_RATE_LIMIT=60/min/IP — not PAID's 10/min, which would break ~5s poll-manager polling and dead-end an in-flight job) before the AtlasCloud/Kling call; bounds job-ID enumeration + DoS amplification. +test asserts the 429 path skips the provider + per-IP isolation | H/security | Run 29 |
| #188 | docs: disclose the enforced 50-export/day fair-use ceiling in the ASO App Store listing (was "Unlimited exports" — Apple/FTC accuracy) + correct README's "on-device Vision/CoreML fallback" (no .mlmodel is bundled → Vision-only in the shipped build). LIVING-ARTIFACTS honesty, verified against spend-ceiling.ts + a repo-wide .mlmodel glob | D/honesty | Run 29 |
| #187 | feat(H5): render the Cloudflare Turnstile widget on the landing WaitlistForm (gated on NEXT_PUBLIC_TURNSTILE_SITE_KEY) so bot protection works END-TO-END when enabled — the backend already verified the token, but the form never sent one, so setting the secret would have 400'd every signup (latent BUILDS≠WORKS). + CSP frame-src for the challenge iframe. Completes H5 | H/security | Run 29 |
| #185 | test(web): cover the time-decay + capacity edges of the wallet-drain & cache controls — spend-ceiling 24h window reset (+ must-not-reset-early), rate-limit sliding-window decay (full + partial), asset-cache LRU eviction + expired-purge, detection-cache source-fingerprint invalidation. +12 tests (619→631); previously these abuse/cache controls were only tested at a single instant | G/tests | Run 28 |
| #184 | fix(web): set maxDuration=300 on /api/plan so the SSE planner (Sonnet, 1–3 min adaptive thinking) isn't killed at Vercel's platform default mid-stream — silent "Failed to fetch"/lost export. Brings the web planner to parity with /api/ios-plan (already 300). Functional-reality fix | B/reliability | Run 28 |
| #180 | security(ios): hard-disable the ElevenLabs + AtlasCloud direct-to-provider key paths (apiKey→nil, isAvailable→false) — closes the scorecard's #1 store_readiness blocker (App Store credential-handling risk) + a server-gate bypass; a previously-untracked gap (REMAINING_STEPS 0a only covered the 4 Anthropic services) | H/store | Run 27 |
| #179 | fix(poll-manager): fan out duplicate-predictionId waiters via a per-task array instead of mutating a shared resolve/reject callback chain — closes the scorecard correctness_reliability callback-race finding | G/correctness | Run 27 |
| #170 | feat(cost): meter ElevenLabs + AtlasCloud usage (chars/seconds/job) via [CostMeter] — closes the audio/video COGS-observability blind spot so per-export cost is reconcilable from Vercel logs vs invoices | B/cost | Run 26 |
| #171 | a11y: label waitlist input (aria-label/autocomplete), announce form result (role=status/alert), wire FAQ aria-controls (always-rendered panel), focus-visible rings on landing | G/a11y | Run 26 |
| #156 | feat(H6): per-request nonce-based Content-Security-Policy (script-src nonce + strict-dynamic, no unsafe-inline) — closes the last H6 gap | H6 | Run 25 |
| #158 | fix(honesty): disclose Pro's daily fair-use cap so "unlimited" isn't misleading (card → "Unlimited monthly exports"; FAQ reworded) | D/honesty | Run 25 |
| #151 | fix(P0): meter per-export API cost on both validation routes (/api/validate streaming + /api/ios-validate) — last unmetered paid LLM call sites | P0 | Run 24 |
| #149 | fix(B6): bound the last untimed external fetches (validate/email/Turnstile) + H5 CAPTCHA-required test | B6 | Run 23 |
| #137 | feat(H7): per-user daily generation ceiling on all paid sub-call routes + gate /api/ios-validate | H7 | Run 22 |
| #138 | fix(D): correct App Store metadata to honest business-paid model (no BYOK, cloud-first, uses gen-AI) | D | Run 22 |
| #139 | feat(G4/G6): capture + commit journey screenshots (see what the user sees) | G4/G6 | Run 22 |
| #141 | feat(design): rework the free-limit paywall into an intentional, on-brand conversion moment | Design | Run 22 |
| #127 | fix(D/C2): align iOS StoreKit + metadata pricing to $14.99/$149.99 | D/C2 | Run 21 |
| #126 | fix(D/F): align web + ASO pricing to $14.99/$149.99 + annual line | D/F | Run 21 |
| #125 | test(G2): cover analytics.trackEvent (SSR-safety, dispatch, prop cleaning) | G2 | Run 21 |
| #124 | feat(H2/H3): route hardening — generic errors + array bounds on paid routes | H2/H3 | Run 21 |
| #123 | feat(E6): Growth Execution engine — waitlist double-opt-in + email/social/metrics plumbing | E6 | Run 21 |
| #115 | meta(Run 20): post-#114 — tick P0 server-side-entitlement bullet + C1; mark iOS send-side done | meta | pending #115 |
| #114 | feat(P0/C1): iOS attaches StoreKit signed transaction (result.jwsRepresentation) to ios-score/ios-plan — completes the entitlement loop | P0/C1 | Run 20 |
| #113 | meta(Run 20): housekeeping — fix stale APP_STORE/product-id docs; tick H2; record #110–#112; iOS send-side queued | meta | Run 20 |
| #112 | test(G2): cover proxy-video + animate/check routes; fold ElevenLabs ceiling/NaN/empty cases into elevenlabs.test.ts | G2 | Run 20 |
| #111 | feat(H2): server-side input-size bounds on paid routes before the provider call (shared input-bounds.ts) | H2 | Run 20 |
| #110 | feat(P0/C1): real server-side App Store JWS entitlement verification (x5c chain + ES256 + Pro-SKU/expiry/revocation) | P0/C1 | Run 20 |
| #109 | meta(Run 19): housekeeping — corrects false P0-done claim; LOOP_MEMORY, IMPROVEMENT_LOG, REMAINING_STEPS, ROADMAP | meta | pending #109 |
| #108 | feat(H2): bound clips/clipFrames payload (MAX_FILES) before the paid validate calls | H2 | 2026-06-27 |
| #107 | fix(D): correct stale BYOK claims in Terms — describe the business-paid AI model | D | 2026-06-27 |
| #106 | feat(H1): rate-limit the 13 remaining paid/expensive backend routes | H1 | 2026-06-27 |
| #105 | feat(P0/H1): remove LAST embedded Anthropic key from TapeValidationService; add /api/ios-validate (+rate limit) — rescued stuck #100/#84 | P0/H1 | 2026-06-27 |
| #104 | ROADMAP Track E: add E6 — Growth EXECUTION engine (server-side, in web/) | E6 | 2026-06-27 |
| #103 | meta(Run 18): housekeeping — LOOP_MEMORY, IMPROVEMENT_LOG, REMAINING_STEPS | meta | 2026-06-27 |
| #102 | feat(H6): security headers — HSTS, X-Content-Type-Options, X-Frame-Options, CORS; consolidate from vercel.json | H6 | 2026-06-27 |
| #101 | feat(H): rate limiting (H1), spend ceiling (H7), CAPTCHA (H5), validate quota gate, error hygiene (H3) | H1/H3/H5/H7 | 2026-06-27 |
| #100 | feat(P0): rescue ios-validate (TapeValidationService) | P0 | CLOSED — stuck (stale base + Swift type error); superseded by #105 |
| #91 | meta(Run 16): housekeeping — LOOP_MEMORY, IMPROVEMENT_LOG, REMAINING_STEPS | meta | 2026-06-26 |
| #90 | test(G2): add route tests for /api/ios-score (6 tests) | G2 | 2026-06-26 |
| #89 | feat(G3): add gaming/esports highlight detection fixture | G3 | 2026-06-26 |
| #88 | feat(security): optional userId quota gate for /api/validate | security/P0 | CLOSED — logic folded into #101 |
| #87 | fix(ios): remove force-unwrap in ConfettiView; align fallback subscription prices | A3/C2 | 2026-06-26 |
| #86 | meta(Run 15): housekeeping — LOOP_MEMORY, IMPROVEMENT_LOG, REMAINING_STEPS | meta | 2026-06-26 |
| #85 | feat(P0): remove embedded API key from AIEffectRecommendationService; add /api/ios-plan backend | P0 | 2026-06-26 |
| #84 | feat(P0): remove embedded Anthropic key from TapeValidationService; add /api/ios-validate backend | P0 | CLOSED — never merged; superseded by #105 (Run 19) |
| #83 | feat(P0): remove embedded Anthropic key from ClaudeVisionService (disable direct calls) | P0 | 2026-06-26 |
| #82 | meta(Run 14): housekeeping — LOOP_MEMORY, IMPROVEMENT_LOG, REMAINING_STEPS | meta | 2026-06-26 |
| #81 | feat(G3): add cooking-highlight eval fixture for detect.eval.ts auto-discovery | G3 | 2026-06-26 |
| #80 | feat(P0): remove embedded Anthropic key from iOS; route frame scoring through /api/ios-score | P0 | 2026-06-26 |
| #79 | feat(P0): add /api/ios-score — iOS Haiku scoring proxy, removes embedded API key | P0 | 2026-06-26 |
| #78 | test(G2): extract mergeDuckSegments from audio-mux + 12 Vitest tests | G2 | 2026-06-26 |
| #77 | test(G2): export pure functions from frame-extractor + 29 Vitest tests | G2 | 2026-06-26 |
| #76 | meta(Run 13): housekeeping — IMPROVEMENT_LOG, LOOP_MEMORY, REMAINING_STEPS | meta | 2026-06-25 |
| #75 | feat(ios): P0 — BackendConfig URL resolver (prerequisite for key removal) | P0 | 2026-06-25 |
| #74 | feat(evals): G3 — travel fixture + auto-discover eval fixtures | G3 | 2026-06-25 |
| #73 | test(web): G2 — PR #61 route tests (24 tests, 4 routes) | G2 | 2026-06-25 |
| #72 | test(web): G2 — VercelKVQuotaStore unit tests (14 tests) | G2 | 2026-06-25 |
| #71 | BUSINESS_CASE: add machine-readable BUSINESS_CASE_SUMMARY block + floor flag | F1/meta | 2026-06-25 |
| #70 | ROADMAP: add LIVING ARTIFACTS operating principle | meta | 2026-06-25 |
| #69 | ROADMAP F9: make docs/BUSINESS_CASE.md a TRULY LIVING artifact (recompute, don't rot) | F1/meta | 2026-06-25 |
| #68 | meta(Run 12): housekeeping — deep audit record, LOOP_MEMORY, IMPROVEMENT_LOG, REMAINING_STEPS | meta | 2026-06-25 |
| #67 | test(G2): add route tests for /api/validate and /api/waitlist (12 tests) | G2 | 2026-06-25 |
| #66 | feat(B3): add Vercel KV-backed quota store; wire into getQuotaStore() | B3 | 2026-06-25 |
| #65 | test(G2): add coverage for post-processing pure functions (28 tests) | G2 | 2026-06-25 |
| #64 | test(G2): add coverage for ai-models cost estimation (17 tests) | G2 | 2026-06-25 |
| #63 | test(G2): add Vitest coverage thresholds (lines/functions/branches ≥60/60/50%) | G2 | 2026-06-25 |
| #62 | security: patch Vitest CRITICAL CVE + Vite HIGH CVEs via npm audit fix | security | 2026-06-25 |
| #61 | P0: add entitlement gate to all 8 remaining ungated paid API routes | P0/B3 | 2026-06-25 |
| #58 | Evals: add golden fixture + env-gated detection quality eval | G3 (partial) | 2026-06-25 |
| #57 | P0 (iOS): remove BYOK API-key input from SettingsView | P0 | 2026-06-25 |
| #56 | P0: add entitlement gate to /api/plan (Sonnet planner route) | P0/B3 | 2026-06-25 |
| #55 | P0: add entitlement gate to /api/sfx, /api/voiceover, /api/music routes | P0/B3 | 2026-06-25 |
| #54 | ROADMAP: add Track G (world-class quality, validation & evals) + DoD gate | meta | 2026-06-25 |
| #53 | P0: server-side entitlement gate + iOS frame-scoring proxy (business-paid) | P0/B3 | 2026-06-25 |
| #52 | P0 DECISION: BUSINESS-PAID (not BYOK) — correct the factory's wrong guess | P0/meta | 2026-06-25 |
| #51 | ROADMAP: add Progress format contract; make Definition-of-Done items checkboxes | meta | 2026-06-25 |
| #50 | meta(Run 10): housekeeping — loop state, improvement log, business case update | meta | 2026-06-25 |
| #49 | E5: Add Plausible analytics module + wire landing page conversion events | E5 | 2026-06-25 |
| #48 | E4: Add content calendar + 12 post scripts (8-week launch plan) | E4 | 2026-06-25 |
| #47 | E3: Add App Store Optimization package (name, keywords, description, screenshots) | E3 | 2026-06-25 |
| #46 | E2: Add brand kit guide — colors, type, voice, logo & social assets spec | E2 | 2026-06-25 |
| #45 | B4: Switch planner from Opus 4.8 → Sonnet 4.6 (−80% COGS per export) | B4 | 2026-06-25 |
| #42 | Add E1 marketing landing page at /landing + /api/waitlist email capture endpoint | E1 | 2026-06-24 |
| #37 | Fix A3: replace 8 URL force-unwraps in ElevenLabsService with guard-let | A3 | 2026-06-24 |
| #36 | Fix A3: missing introCardEnabled/outroCardEnabled in AppState; remove force-unwraps | A3 | 2026-06-24 |
| #34 | Fix: use Haiku for iOS frame scoring instead of Sonnet (B2/B4) | B2/B4 | 2026-06-24 |
| #33 | Housekeeping: update loop state for Run 7 | meta | 2026-06-24 |
| #32 | Add Terms of Use + Support pages; fix inaccurate web metadata (D3) | D3 | 2026-06-24 |
| #31 | Fix StoreKit entitlement sync to AppState at launch and restore (C1/C2) | C1/C2 | 2026-06-24 |
| #28 | Add CLAUDE_VALIDATOR cost metering to ai-models.ts (B2) | B2 | 2026-06-24 |
| #26 | Fix iOS: update Sonnet model ID + unblock main thread in voice clone | A3 | 2026-06-24 |
| #25 | Fix CLAUDE_PLANNER to valid model ID claude-opus-4-8 | B2 | 2026-06-24 |
| #24 | Housekeeping: update loop state for Run 4 | meta | 2026-06-24 |
| #23 | Remove force-unwrap on baseAddress in AudioFeatureService FFT pack | A3 | 2026-06-24 |
| #22 | Fix AppStoreMetadata.swift false on-device/no-upload claims | D4 | 2026-06-24 |
| #20 | Fix Swift 6 strict-concurrency error in StoreKitService | A3 | 2026-06-24 |
| #19 | Cap base frame extraction per video to 120 frames | B2 | 2026-06-24 |
| #18 | Housekeeping: update loop state for Run 2 | meta | 2026-06-24 |
| #17 | Add per-call cost metering for all Claude API calls | B2 | 2026-06-24 |
| #15 | Make iOS CI green via SwiftPM test target | A1 | 2026-06-24 |
| #14 | Add housekeeping docs: IMPROVEMENT_LOG, PENDING_OPS, LOOP_MEMORY | meta | 2026-06-24 |
| #13 | Remove fatalError crash in UserAccountService init | A3 | 2026-06-24 |
| #12 | Honest privacy policy with third-party API disclosure | D1 | 2026-06-24 |
| #11 | Centralize AI model IDs in ai-models.ts | B4 | 2026-06-24 |
| #10 | Add cost-optimized model selection mandate + MODEL_COSTS.md | B4 | 2026-06-24 |
| #9 | Add CI status badge to README | meta | 2026-06-24 |
| #8 | iOS/web feature parity: validation loop, photo animation, production plan | A + B1 | 2026-06-24 |
| #7 | Enhance frame scoring, photo handling, export pipeline | B1 + B2 | 2026-03-19 |
| #6 | Validation loop with Haiku review + targeted asset regeneration | B1 | 2026-03-15 |
| #5 | Merge | — | 2026-03-08 |
| #4 | Fix SFX rate limiting; auto-retry rejected music prompts | B1 | 2026-03-08 |
| #3 | Add photo animation (Kling) + AI music generation (ElevenLabs) | A2 + B1 | 2026-03-07 |
| #2 | Add cloud-first AI effect recommendation + unified tape planning | A2 + B1 | 2026-03-05 |
| #1 | Add Highlight Magic MVP (iOS + web) | all | 2026-02-23 |

## Pending (open PRs — not yet logged; add to table above once merged)
- PR #16: A1 iOS CI destination fix — DO NOT MERGE (edits .github/ + Swift syntax bug; needs owner close)
- PR #84: CLOSED Run 18 (replaced by PR #100)
- PR #88: CLOSED Run 18 (logic incorporated into PR #101)
- PR #91: meta Run 16 housekeeping — pending CI merge
- PR #97: CLOSED Run 18 (stale; replaced by PR #103)
- PR #100: P0 ios-validate rescue (Run 18) — pending CI merge
- PR #101: Track H security hardening (Run 18) — pending CI merge
- PR #102: H6 security headers (Run 18) — pending CI merge
- PR #103: meta Run 18 housekeeping — pending CI merge
